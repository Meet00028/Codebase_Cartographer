{
  "name": "cartographer",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev -- --port 5173",
    "dev:backend": "cd backend && source venv/bin/activate && uvicorn main:app --reload --port 8001",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
